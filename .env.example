# Postgres connection string. On Railway, attach a Postgres plugin and
# reference it as ${{ Postgres.DATABASE_URL }} in the service variables.
DATABASE_URL="postgresql://user:password@host:5432/habits?schema=public"
